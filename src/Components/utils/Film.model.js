export default class FilmModel {
  constructor(title, shortName, synopsys, id, agecert) {
    this.title = title;
    this.shortName = shortName;
    this.synopsys = synopsys;
    this.id = id;
    this.agecert = agecert;
  }
}
